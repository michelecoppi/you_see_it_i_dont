-- This module is the single source of truth for the build shipped to players.
-- Bump both values when cutting a new reproducible baseline.
return table.freeze({
	Version = "0.1.0-baseline.1",
	BuildId = "baseline-2026-08-02.1",
	Channel = "Baseline",
	DisplayVersion = "0.1.0-baseline.1 (baseline-2026-08-02.1)",
})
